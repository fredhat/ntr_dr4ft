let d = React.DOM

export let STRINGS = {
  BRANDING: {
    SITE_NAME: ['draft', 'netrunner'],
    DEFAULT_USERNAME: 'user',
  },

  PAGE_SECTIONS: {
    MOTD: null, // message of the day; can be a React element

    FOOTER: d.div({},
      d.strong({}, 'draft.netrunner'),
      ' is a fork of the ',
      d.code({}, 'dr4ft'),
      ' project by tritoch. Contributions welcome! ',
      d.a({ href: 'https://github.com/tritoch/dr4ft' },
        'https://github.com/tritoch/dr4ft')),
  },
}
